{% extends ('Site/base.html.twig') %}

{% block bodyClass %}home{% endblock %}

{% block content %}
    {% set bannerSpeedLinksType = constant('App\\Entity\\Banner::TYPE_SPEED_LINKS') %}
    {% set homePageProducts = get_class_constants('App\\Entity\\ProductOptions') %}

    <!-- home2-slider-start -->
    <div class="home3-slider-container">
        <!-- Slider Image -->
        <div id="home3_slider" class="nivoSlider slider-image">
            {% for slider in sliders %}
                <a class="hvr-bs" href="{{ slider['button_link'] }}">
                    <img src="{{ is_mobile() ? slider.mobile_image_link : slider.image_link }}" alt="" title="#{{ slider.id }}" />
                </a>
            {% endfor %}
        </div>
        {% for slider in sliders %}
            <!-- Slider Caption {{ slider['id'] }} -->
            <div id="{{ slider['id'] }}" class="nivo-html-caption slider-caption-{{ slider['id'] }}">
                <div class="slider-progress"></div>
                <div class="container-fluid">
                    <div class="row">
                        {% set sliderPosition = '' %}
                        {% if is_mobile() or is_tablet() %}
                            {% set sliderPosition = 'text-center' %}
                        {% endif %}

                        <div class="col-xs-12 {{ sliderPosition }}">
                                <div class="slide3-text">
                                    <div class="middle-text">
                                        {% if slider['descriptions'][0] is defined %}
                                            <div class="cap-dec animated bounceIn">
                                                {{ slider['descriptions'][0] is defined ? slider['descriptions'][0] | raw : '&nbsp;' }}
                                            </div>
                                        {% endif %}
                                        {% if slider['descriptions'][1] is defined %}
                                            <div class="cap-title animated zoomIn">
                                                {{ slider['descriptions'][1] is defined ? slider['descriptions'][1] | raw : '&nbsp;' }}
                                                {% endif %}
                                            </div>
                                        {% if slider['descriptions'][2] is defined %}
                                            <div class="cap-title-2 animated zoomIn">
                                                {{ slider['descriptions'][2] is defined ? slider['descriptions'][2] | raw : '&nbsp;' }}
                                            </div>
                                        {% endif %}
                                    </div>
                                </div>
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    <!-- home2-slider-end -->
    <!-- featured-itema-area-start -->
    <div class="home-2-featured-item-area">
        <div class="container">

            <div class="featured-items">
                <div>
                    <div class="featured-all">
                        <div class="fi-row">
                            <div class="row">
                                <div id="top_carousel" class="feature-pro-carousel owl-carousel">
                                    {% import 'Site/Macros/productGrid.html.twig' as productGrid %}
                                    {% for product in products[homePageProducts.HOME_PAGE_UP] %}
                                        {{ productGrid.productGrid(product, 'col-xs-12', locale, countryDefaultLocale) }}
                                    {% endfor %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- featured-itema-area-end -->
    <!-- home3-banner-area-start -->
    <div class="home3-banner-area">
        <div class="container">
            <div class="row home3-row m-bt-3-xs">
                <div class="col-md-4 col-sm-6 col-xs-12 m-bt-2-xs home3-banner-column">
                    {% if banners[bannerSpeedLinksType][1] is defined %}
                        {% set firstBanner = banners[bannerSpeedLinksType][1] %}
                        <a href="{{ firstBanner.translations[locale].button_link }}">
                            <div class="h3b-single-banner-4 h3b-single-banner">
                                <div class="h3b-img">
                                    <img src="{{ firstBanner.media.images.desktop.file }}" alt="{{ default_alt_tag }}">
                                </div>
                                <div class="h3b-content-banner-4 h3b-content">
                                    <h3>{{ firstBanner['description'][0] is defined ? firstBanner['description'][0] : '&nbsp;' }}</h3>
                                    <h2>{{ firstBanner['description'][1] is defined ? firstBanner['description'][1] : '&nbsp;' }}</h2>
                                </div>
                            </div>
                        </a>
                    {% endif %}
                </div>
                <div class="col-md-4 hidden-sm hidden-xs home3-banner-column home-middle-banners">
                    {% if banners[bannerSpeedLinksType][2] is defined %}
                        {% set secondBanner = banners[bannerSpeedLinksType][2] %}
                        <a href="{{ secondBanner.translations[locale].button_link }}">
                            <div class="h3b-single-banner-3 h3b-single-banner">
                                <div class="h3b-img">
                                    <img src="{{ secondBanner.media.images.desktop.file }}" alt="{{ default_alt_tag }}">
                                </div>
                                <div class="h3b-content-banner-3 h3b-content">
                                    <h2>{{ secondBanner['description'][0] is defined ? secondBanner['description'][0] : '&nbsp;' }}</h2>
                                </div>
                            </div>
                        </a>
                    {% endif %}
                    {% if banners[bannerSpeedLinksType][3] is defined %}
                        {% set thirdBanner = banners[bannerSpeedLinksType][3] %}
                        <a href="{{ thirdBanner.translations[locale].button_link }}">
                            <div class="h3b-single-banner-3 h3b-single-banner">
                                <div class="h3b-img">
                                    <img src="{{ thirdBanner.media.images.desktop.file }}" alt="">
                                </div>
                                <div class="h3b-content-banner-3 h3b-content">
                                    <h2>{{ thirdBanner['description'][0] is defined ? thirdBanner['description'][0] : '&nbsp;' }}</h2>
                                </div>
                            </div>
                        </a>
                    {% endif %}
                </div>
                <div class="col-md-4 col-sm-6 col-xs-12 home3-banner-column">
                    {% if banners[bannerSpeedLinksType][4] is defined %}
                        {% set fourthBanner = banners[bannerSpeedLinksType][4] %}
                        <a href="{{ fourthBanner.translations[locale].button_link }}">
                            <div class="h3b-single-banner-4 h3b-single-banner">
                                <div class="h3b-img">
                                    <img src="{{ fourthBanner.media.images.desktop.file }}" alt="{{ default_alt_tag }}">
                                </div>
                                <div class="h3b-content-banner-4 h3b-content">
                                    <h3>{{ fourthBanner['description'][0] is defined ? fourthBanner['description'][0] : '&nbsp;' }}</h3>
                                    <h2>{{ fourthBanner['description'][1] is defined ? fourthBanner['description'][1] : '&nbsp;' }}</h2>
                                </div>
                            </div>
                        </a>
                    {% endif %}
                </div>
            </div>
            <div class="row home3-row">
                <div class="hidden-lg hidden-md col-xs-12 home3-banner-column home-middle-banners">
                    {% if banners[bannerSpeedLinksType][2] is defined %}
                        {% set secondBanner = banners[bannerSpeedLinksType][2] %}
                        <a href="{{ secondBanner.media.images.desktop.file }}">
                            <div class="h3b-single-banner-3 h3b-single-banner">
                                <div class="h3b-img">
                                    <img src="{{ secondBanner.media.images.desktop.file }}" alt="{{ default_alt_tag }}">
                                </div>
                                <div class="h3b-content-banner-3 h3b-content">
                                    <h2>{{ secondBanner['description'][0] is defined ? secondBanner['description'][0] : '&nbsp;' }}</h2>
                                </div>
                            </div>
                        </a>
                    {% endif %}
                </div>
                    <div class="hidden-lg hidden-md col-xs-12 home3-banner-column home-middle-banners">
                    {% if banners[bannerSpeedLinksType][3] is defined %}
                        {% set thirdBanner = banners[bannerSpeedLinksType][3] %}
                        <a href="{{ thirdBanner.media.images.desktop.file }}">
                            <div class="h3b-single-banner-3 h3b-single-banner">
                                <div class="h3b-img">
                                    <img src="{{ thirdBanner.media.images.desktop.file }}" alt="">
                                </div>
                                <div class="h3b-content-banner-3 h3b-content">
                                    <h2>{{ thirdBanner['description'][0] is defined ? thirdBanner['description'][0] : '&nbsp;' }}</h2>
                                </div>
                            </div>
                        </a>
                    </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
    <!-- home3-banner-area-end -->
    <!-- featured-itema-area-start -->
    <div class="home-2-featured-item-area">
        <div class="container">

            <div class="featured-items">
                <div>
                    <div class="featured-all">
                        <div class="fi-row">
                            <div class="row">
                                <div id="bottom_carousel" class="feature-pro-carousel owl-carousel">
                                    {% import 'Site/Macros/productGrid.html.twig' as productGrid %}
                                    {% for product in products[homePageProducts.HOME_PAGE_DOWN] %}
                                        {{ productGrid.productGrid(product, 'col-xs-12', locale, countryDefaultLocale) }}
                                    {% endfor %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- featured-itema-area-end -->
    <!-- home3-newsletter-area-start -->
    {% set loyaltyType = constant('App\\Entity\\Banner::TYPE_LOYALTY') %}
    {% if null != banners[loyaltyType] %}
        {% set bannerLink = "background: rgba(0, 0, 0, 0) url("~ banners[loyaltyType].media.images[mediaDevice].file ~") no-repeat scroll left top / cover" %}
        <a href="{{ banners[loyaltyType].translations[locale].button_link }}" class="home3-newsletter-area" style="{{ bannerLink }}"></a>
    {% endif %}
    <!-- home3-newsletter-area-end -->
{% endblock %}
{% block pageStyleSheets %}
    {{ encore_entry_link_tags('css/site/pages/home') }}
{% endblock %}
