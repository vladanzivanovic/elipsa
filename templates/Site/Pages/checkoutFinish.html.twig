{% extends 'Site/base.html.twig' %}

{% set title = true == is_successful_transaction ? 'title.checkout_completed_successful' | trans : 'title.checkout_completed_unsuccessful' | trans %}


{% block title %}{{ title }}{% endblock %}
{% block bodyClass %}order-finish-page{% endblock %}


{% block content %}
    {{ include('Site/Components/breadcrumb.html.twig', {'pages': [
        {'title': 'cart.checkout' | trans}
    ], 'activePage': title ~' #'~ id}) }}

{#    {{ include('Site/Components/breadcrumb.html.twig', {'pages': [], 'activePage': title ~' #'~ id}) }}#}

    <div class="order-tracker-area">
        <div class="container">
            <div class="row">
                <div class="col-sm-12 col-xs-12">
                    <div class="order-tracker">
                        <h2 class="ot-title">{{ title }}</h2>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {{ include('Site/Components/orderFinishData.html.twig') }}

{% endblock %}
{% block pageStyleSheets %}
    {{ encore_entry_link_tags('css/site/pages/orderFinish') }}
{% endblock %}
